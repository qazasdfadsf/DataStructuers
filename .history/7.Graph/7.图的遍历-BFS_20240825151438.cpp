#include "init.h"
#include "1.邻接矩阵法.cpp"
#define  MAX_VERTEX_NUM false
bool visited[MAX_VERTEX_NUM];
void BFS(Graph G,int v){
    visit(v);
    visited[v]=TRUE;
    Enqueue(Q,v);
    while(!isEmpty(Q)){
        DeQueue(Q,v);
        for(w)
    }
}